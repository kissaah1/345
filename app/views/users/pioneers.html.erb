<% provide(:title, "Pioneers") %>

<section class="centered index-section">
	<div class="row">
		<div class="columns grid-80 centered clearfix">
			<div class="grid-100">
					<h2><i class="material-icons">near_me</i>Pioneers</h2>
					<ul class="users">
					<% @pioneers.each do |user| %>
					<%= render user %>
					<% end %>
					</ul>
					<%= will_paginate @pioneers %>

			</div><!-- grid-100 -->
		</div><!-- columns -->
	</div><!-- row -->
</section><!-- index-section -->