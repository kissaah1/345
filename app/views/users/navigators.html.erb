<% provide(:title, "Navigators") %>


<section class="centered index-section">
	<div class="row">
		<div class="columns grid-80 centered clearfix">
			<div class="grid-100">
					<h2><i class="material-icons">explore</i>Navigators</h2>
					<ul class="users">
					<% @navigators.each do |user| %>
					<%= render user %>
					<% end %>
					</ul>
					<%= will_paginate @navigators %>

			</div><!-- grid-100 -->
		</div><!-- columns -->
	</div><!-- row -->
</section><!-- index-section -->
